PORT=3050

# worldcup26.ir JWT token (optional for public endpoints)
# Register at https://worldcup26.ir/auth/register then paste token here
WC_JWT_TOKEN=

# football-data.org API key (for live minute + squad data)
FOOTBALL_DATA_API_KEY=bccb824e503f4661bd1965efe4cdb953

# Optional: overrides the cookie-signing secret (default is hardcoded & stable).
# Set a custom value to invalidate all existing 30-day sessions.
# AUTH_SECRET=

# Page passwords (hardcoded in src/auth.js):
#   artes2026admin → admin (preview + send to vMix)
#   artes2026      → viewer (preview only)
